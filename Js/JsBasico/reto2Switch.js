
// var piedra = 'Piedra';

// var papel = 'Papel';
// var tijera = 'Tijera';

// function juego(user, cpu) {
//   switch (user===user && cpu===cpu) {
//     case user === cpu:
//       console.log(`User:${user} -  CPU: ${cpu} ambos tienen el mismo objeto, es un EMPATE`);
//       break;
//     case user === piedra && cpu === tijera:
//       console.log(`User: ${user} -  CPU: ${cpu} Usuario tiene la ventaja, Ganador, USUARIO`);
//       break;
//     case user === papel && cpu === piedra:
//       console.log(`User: ${user} -  CPU: ${cpu} Usuario tiene la ventaja, Ganador, USUARIO`);
//       break;
//     case user === tijera && cpu === papel:
//       console.log(`User: ${user} -  CPU: ${cpu} Usuario tiene la ventaja, Ganador, USUARIO`);
//       break;
//     default:
//       console.log(`User: ${user} -  CPU: ${cpu} CPU tiene la ventaja, Ganador, CPU`);
//       break;
//   }
// }
// juego(tijera, piedra);


switch (false) {
  case false:
    console.log('Falso');
    break
}