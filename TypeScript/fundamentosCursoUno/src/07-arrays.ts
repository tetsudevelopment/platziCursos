(() => {
  let price = [1, 2, 3, 1, 2, 3, "hola",true]
  // price.push('asd');
  // price.push(true);
  // price.push({});
  price.push(1,213,2,)

  let products = ["hola", true];
  products.push(false);

  const mixed: (number | string | boolean)[] = ["hola", true];
  mixed.push(21);

  let numbers = [1, 2, 3, 1, 2,]
  numbers.map(item => item * 2);
  
})()
